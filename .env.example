# Docker Compose (default — works with: docker compose up)
DATABASE_URL=postgresql+asyncpg://dev:dev@db:5432/travel_planner

# Local development (change @db to @localhost)
# DATABASE_URL=postgresql+asyncpg://dev:dev@localhost:5432/travel_planner

# Basic Auth credentials
API_USERNAME=admin
API_PASSWORD=secret
